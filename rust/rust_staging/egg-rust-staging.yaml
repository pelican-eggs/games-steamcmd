_comment: 'DO NOT EDIT: FILE GENERATED AUTOMATICALLY BY PANEL'
meta:
  version: PLCN_v3
  update_url: null
exported_at: '2026-05-10T01:59:16+00:00'
name: 'Rust Staging'
author: root@smc.li
uuid: e60b1795-764f-4508-b820-584f5335e66d
description: |-
  The only aim in Rust is to survive. To do this you will need to overcome struggles such as hunger,
  thirst and cold. Build a fire. Build a shelter. Kill animals for meat. Protect yourself from other
  players, and kill them for meat. Create alliances with other players and form a town. Do whatever it
  takes to survive.
icon: null
tags:
  - steamcmd
features:
  - steam_disk_space
docker_images:
  'ghcr.io/pterodactyl/games:rust': 'ghcr.io/pterodactyl/games:rust'
file_denylist: {  }
startup_commands:
  Default: './RustDedicated -batchmode +server.port {{SERVER_PORT}} +server.queryport {{QUERY_PORT}} +server.identity "rust" +rcon.port {{RCON_PORT}} +rcon.web true +server.maxplayers {{MAX_PLAYERS}} +rcon.password "{{RCON_PASS}}" +server.saveinterval {{SAVEINTERVAL}} +app.port {{APP_PORT}} {{ADDITIONAL_ARGS}}'
config:
  files:
    server/rust/cfg/server.cfg:
      parser: file
      find:
        'server.hostname ': 'server.hostname "{{server.environment.HOSTNAME}}"'
        'server.description ': 'server.description "{{server.environment.DESCRIPTION}}"'
        'server.url ': 'server.url "{{server.environment.SERVER_URL}}"'
        'server.headerimage ': 'server.headerimage "{{server.environment.SERVER_IMG}}"'
        'server.logoimage ': 'server.logoimage "{{server.environment.SERVER_LOGO}}"'
        'server.level ': 'server.level "{{server.environment.LEVEL}}"'
        'server.worldsize ': 'server.worldsize {{server.environment.WORLD_SIZE}}'
        'server.seed ': 'server.seed {{server.environment.WORLD_SEED}}'
        'server.tags ': 'server.tags "{{server.environment.SERVER_TAGS}}"'
        'server.levelurl ': 'server.levelurl "{{server.environment.MAP_URL}}"'
  startup:
    done: 'Server startup complete'
  logs: {  }
  stop: quit
scripts:
  installation:
    script: |
      #!/bin/bash
      # steamcmd Base Installation Script
      #
      # Server Files: /mnt/server
      # Image to install with is 'ghcr.io/parkervcp/installers:debian'

      ##
      #
      # Variables
      # STEAM_USER, STEAM_PASS, STEAM_AUTH - Steam user setup. If a user has 2fa enabled it will most likely fail due to timeout. Leave blank for anon install.
      # WINDOWS_INSTALL - if it's a windows server you want to install set to 1
      # SRCDS_APPID - steam app id found here - https://developer.valvesoftware.com/wiki/Dedicated_Servers_List
      # SRCDS_BETAID - beta branch of a steam app. Leave blank to install normal branch
      # SRCDS_BETAPASS - password for a beta branch should one be required during private or closed testing phases.. Leave blank for no password.
      # INSTALL_FLAGS - Any additional SteamCMD  flags to pass during install.. Keep in mind that steamcmd auto update process in the docker image might overwrite or ignore these when it performs update on server boot.
      # AUTO_UPDATE - Adding this variable to the egg allows disabling or enabling automated updates on boot. Boolean value. 0 to disable and 1 to enable.
      #
       ##

      # Install packages. Default packages below are not required if using our existing install image thus speeding up the install process.
      #apt -y update
      #apt -y --no-install-recommends install curl lib32gcc-s1 ca-certificates

      ## just in case someone removed the defaults.
      if [[ "${STEAM_USER}" == "" ]] || [[ "${STEAM_PASS}" == "" ]]; then
          echo -e "steam user is not set.\n"
          echo -e "Using anonymous user.\n"
          STEAM_USER=anonymous
          STEAM_PASS=""
          STEAM_AUTH=""
      else
          echo -e "user set to ${STEAM_USER}"
      fi

      ## download and install steamcmd
      cd /tmp
      mkdir -p /mnt/server/steamcmd
      curl -sSL -o steamcmd.tar.gz https://steamcdn-a.akamaihd.net/client/installer/steamcmd_linux.tar.gz
      tar -xzvf steamcmd.tar.gz -C /mnt/server/steamcmd
      mkdir -p /mnt/server/steamapps # Fix steamcmd disk write error when this folder is missing
      cd /mnt/server/steamcmd

      # SteamCMD fails otherwise for some reason, even running as root.
      # This is changed at the end of the install process anyways.
      chown -R root:root /mnt
      export HOME=/mnt/server

      ## install game using steamcmd
      ./steamcmd.sh +force_install_dir /mnt/server +login ${STEAM_USER} ${STEAM_PASS} ${STEAM_AUTH} $( [[ "${WINDOWS_INSTALL}" == "1" ]] && printf %s '+@sSteamCmdForcePlatformType windows' ) +app_update ${SRCDS_APPID} $( [[ -z ${SRCDS_BETAID} ]] || printf %s "-beta ${SRCDS_BETAID}" ) $( [[ -z ${SRCDS_BETAPASS} ]] || printf %s "-betapassword ${SRCDS_BETAPASS}" ) ${INSTALL_FLAGS} validate +quit ## other flags may be needed depending on install. looking at you cs 1.6

      ## set up 32 bit libraries
      mkdir -p /mnt/server/.steam/sdk32
      cp -v linux32/steamclient.so ../.steam/sdk32/steamclient.so

      ## set up 64 bit libraries
      mkdir -p /mnt/server/.steam/sdk64
      cp -v linux64/steamclient.so ../.steam/sdk64/steamclient.so

      ## add below your custom commands if needed

      ## install end
      echo "-----------------------------------------"
      echo "Installation completed..."
      echo "-----------------------------------------"

      ## seed server.cfg for Pelican config.files parser.
      ## the parser overrides existing lines but does NOT append missing ones,
      ## so we have to seed placeholder lines on first install.
      mkdir -p /mnt/server/server/rust/cfg
      CFG=/mnt/server/server/rust/cfg/server.cfg
      if [ ! -s "$CFG" ] || ! grep -q '^server.hostname ' "$CFG"; then
        cat > "$CFG" <<'SEED_EOF'
      server.hostname "default"
      server.description "default"
      server.url ""
      server.headerimage ""
      server.logoimage ""
      server.level "Procedural Map"
      server.worldsize 4500
      server.seed 12345
      server.tags ""
      server.levelurl ""
      SEED_EOF
        echo "  [seed] server.cfg pre-populated for Pelican file parser"
      else
        echo "  [seed] server.cfg already has hostname line; skip"
      fi
    container: 'ghcr.io/parkervcp/installers:debian'
    entrypoint: bash
variables:
  -
    sort: null
    name: Level
    description: 'The world file for Rust to use.'
    env_variable: LEVEL
    default_value: 'Procedural Map'
    user_viewable: true
    user_editable: true
    rules:
      - required
      - string
      - 'max:20'
  -
    sort: null
    name: Description
    description: 'The description under your server title. Commonly used for rules & info. Use \n for newlines.'
    env_variable: DESCRIPTION
    default_value: 'Powered by Pterodactyl'
    user_viewable: true
    user_editable: true
    rules:
      - required
      - string
  -
    sort: null
    name: URL
    description: 'The URL for your server. This is what comes up when clicking the "Visit Website" button.'
    env_variable: SERVER_URL
    default_value: 'http://pterodactyl.io'
    user_viewable: true
    user_editable: true
    rules:
      - nullable
      - url
  -
    sort: null
    name: 'World Size'
    description: 'The world size for a procedural map.'
    env_variable: WORLD_SIZE
    default_value: 3000
    user_viewable: true
    user_editable: true
    rules:
      - required
      - integer
  -
    sort: null
    name: 'World Seed'
    description: 'The seed for a procedural map.'
    env_variable: WORLD_SEED
    default_value: ''
    user_viewable: true
    user_editable: true
    rules:
      - nullable
      - string
  -
    sort: null
    name: 'Max Players'
    description: 'The maximum amount of players allowed in the server at once.'
    env_variable: MAX_PLAYERS
    default_value: 50
    user_viewable: true
    user_editable: true
    rules:
      - required
      - integer
  -
    sort: null
    name: 'Server Image'
    description: 'The header image for the top of your server listing.'
    env_variable: SERVER_IMG
    default_value: ''
    user_viewable: true
    user_editable: true
    rules:
      - nullable
      - url
  -
    sort: null
    name: 'RCON Port'
    description: 'Port for RCON connections.'
    env_variable: RCON_PORT
    default_value: 28016
    user_viewable: true
    user_editable: false
    rules:
      - required
      - integer
  -
    sort: null
    name: 'RCON Password'
    description: 'RCON access password.'
    env_variable: RCON_PASS
    default_value: CHANGEME
    user_viewable: true
    user_editable: true
    rules:
      - required
      - 'regex:/^[\w.-]*$/'
      - 'max:64'
  -
    sort: null
    name: 'Save Interval'
    description: 'Sets the server’s auto-save interval in seconds.'
    env_variable: SAVEINTERVAL
    default_value: 60
    user_viewable: true
    user_editable: true
    rules:
      - required
      - integer
  -
    sort: null
    name: Branch
    description: 'Select the branch to install, such as staging or workcart'
    env_variable: SRCDS_BETAID
    default_value: staging
    user_viewable: true
    user_editable: true
    rules:
      - nullable
      - string
  -
    sort: null
    name: 'App Port'
    description: 'Port for the Rust+ App. -1 to disable.'
    env_variable: APP_PORT
    default_value: 28082
    user_viewable: true
    user_editable: false
    rules:
      - required
      - integer
  -
    sort: null
    name: 'Server Logo'
    description: 'The circular server logo for the Rust+ app.'
    env_variable: SERVER_LOGO
    default_value: ''
    user_viewable: true
    user_editable: true
    rules:
      - nullable
      - url
  -
    sort: null
    name: SRCDS_APPID
    description: ''
    env_variable: SRCDS_APPID
    default_value: 258550
    user_viewable: false
    user_editable: false
    rules:
      - required
      - string
      - 'max:20'
  -
    sort: null
    name: 'Server Name'
    description: 'The name of your server in the public server list.'
    env_variable: HOSTNAME
    default_value: 'A Rust Staging Server'
    user_viewable: true
    user_editable: true
    rules:
      - required
      - string
      - 'max:40'
  -
    sort: 19
    name: 'Server Tags'
    description: 'Steam server browser keywords (comma-separated). Examples: weekly,vanilla,solo,duo,monthly,tr.'
    env_variable: SERVER_TAGS
    default_value: ''
    user_viewable: true
    user_editable: true
    rules:
      - nullable
      - string
      - 'max:128'
